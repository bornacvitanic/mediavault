[package]
name = "mediavault"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mv"
path = "src/main.rs"

[dependencies]
media_core = { path = "../media_core" }
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }

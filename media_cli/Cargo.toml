[package]
name = "media_cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mediavault-cli"
path = "src/main.rs"

[dependencies]
media_core = { path = "../media_core" }
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
serde_json = "1"
